# Dependencies
node_modules

# Build output
dist
output

# Caches
.cache
.vite
*.tsbuildinfo
.eslintcache

# Local env
.env
.env.*
!.env.example

# Python virtualenv (local tooling, not part of the app)
.venv

# Editor / OS
.DS_Store

# Generated map artifacts — built from *.source.json by `npm run build:maps`.
# The source files are the tracked truth; never commit the generated output.
/src/maps/sources/*.json
!/src/maps/sources/*.source.json

# Local reference material (not part of the app)
content/
_archive/
